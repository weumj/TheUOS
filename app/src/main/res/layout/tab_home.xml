<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/univ_pic"
    android:clickable="true"
    android:gravity="center">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"
        android:foreground="@drawable/dim" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/tab_home_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:clipToPadding="false"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:paddingTop="@dimen/tab_home_recycler_view_top_margin"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!--
    <GridView
        android:id="@+id/tab_home_gridview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:cacheColorHint="@android:color/transparent"
        android:columnWidth="100dip"
        android:gravity="center"
        android:horizontalSpacing="3dip"
        android:listSelector="@android:color/transparent"
        android:numColumns="auto_fit"
        android:paddingLeft="@dimen/tab_home_grid_view_horizontal_margin"
        android:paddingRight="@dimen/tab_home_grid_view_horizontal_margin"
        android:paddingTop="@dimen/tab_home_grid_view_vertical_margin"
        android:scrollbars="none"
        android:stretchMode="spacingWidthUniform"
        android:verticalSpacing="10dip"/>
-->
</RelativeLayout>